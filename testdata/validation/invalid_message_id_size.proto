syntax = "proto3";
package test;

import "proto2fixed/binary.proto";

option (binary.fixed) = true;
option (binary.message_id_size) = 3;  // INVALID - should error (must be 1,2,4,8)

message Command {
  option (binary.message_id) = 1;
  uint32 id = 1;
}
